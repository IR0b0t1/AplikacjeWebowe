{{subject}}
<form action="/handleForm" method="GET">
    {{#each fields}}
    <input type="radio" name="show" value="{{name}}">{{name}}</input>
    {{/each}}
    <button type="submit">Submit</button>
</form>